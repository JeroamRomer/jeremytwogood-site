# Copy to .env for local dev (gitignored — never commit real keys).
# In production these are set in the Vercel project's Environment Variables.

# Chat assistant (api/chat.ts) — required for the chat widget to answer.
ANTHROPIC_API_KEY=

# MCP action tools (api/mcp.ts) — optional.
RESEND_API_KEY=
# Booking link used by both the MCP get_availability tool and the chat prompt.
CALENDLY_URL=https://calendly.com/jtwogood
